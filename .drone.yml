clone:
  git:
    image: plugins/git
    pull: true

pipeline:
  test:
    image: alpine
    commands:
      - echo 'fifth branch needs stuff in the beginning'
      - exit 1
      - echo 'success'
      - echo 'that must really be here'
      - echo 'again'
